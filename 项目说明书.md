# ironSpirit 铁人战队官方网站 · 项目说明书

> 本文件是 `ironspirit-team/website` 仓库的项目说明书与网站使用指南，面向新加入的维护者与内容编辑者。
> 内容基于仓库源码整理，最后更新：2026-07-02。

---

## 目录

1. [项目简介](#1-项目简介)
2. [功能总览](#2-功能总览)
3. [技术栈](#3-技术栈)
4. [目录结构](#4-目录结构)
5. [本地开发环境准备](#5-本地开发环境准备)
6. [本地启动与构建](#6-本地启动与构建)
7. [内容编辑指南](#7-内容编辑指南)
8. [路由与页面机制](#8-路由与页面机制)
9. [样式与主题](#9-样式与主题)
10. [部署](#10-部署)
11. [常见问题与维护建议](#11-常见问题与维护建议)
12. [联系方式](#12-联系方式)

---

## 1. 项目简介

**项目名称**：ironSpirit 西南石油大学铁人战队官方网站

**仓库地址**：https://github.com/ironspirit-team/website

**项目性质**：铁人战队的对外门户与对内培训文档站。既承担团队形象展示（首页、团队结构、发展历程、招新入口），又承载智能车培训体系的全部教学文档，并提供博客动态与 AI+ 校内社区入口。

**核心定位**：

- 对外：展示团队简介、实践平台、可参与赛事、加入方式。
- 对内：提供从零开始的智能车学习路径（硬件设计、软件开发、竞赛评估）。
- 内容驱动：所有文档/博客均以 Markdown 编写，由 [Nuxt Content](https://content.nuxt.com) 自动采集为站点页面，无需写 Vue 即可新增内容。

**备案信息**：蜀ICP备2026004009号-3

---

## 2. 功能总览

网站包含四大模块：

| 模块 | 路由 | 入口文件 | 说明 |
| --- | --- | --- | --- |
| 首页 | `/` | `app/pages/index.vue` | Hero 视频头、关于我们、实践平台、AI+ 入口、团队结构、发展历程、铁人协会、加入我们 |
| 智能车文档 | `/docs/smartcar/` | `app/pages/docs/smartcar.vue` | 培训文档中心，左侧自动生成导航，内容来自 `content/docs/smartcar/` |
| 博客动态 | `/blog/` | `app/pages/blog/index.vue` | 按日期倒序的文章列表，内容来自 `content/blog/` |
| AI+ 社区 | `/ai-plus` | `app/pages/ai-plus.vue` | 校内 AI 社区入口（筹备中），Vibe Coding / AI Hackathon / AI 工具 / AI 创作 |

### 首页板块（自上而下）

1. **Hero**：全屏背景视频 + IRONSPIRIT 字母标题 + "ENGINEERED TO INSPIRE" / "EST. 2015"。
2. **关于我们**：战队简介（成立于 2015 年，聚焦机械、嵌入式、视觉、算法、运营）。
3. **实践平台**：4 个平台入口（重点开放性实验、铁人战队协会、创新创业训练项目、科创训练营）+ 6 个可参与比赛方向（RoboMasters、电赛、智能车、机器人创意、睿抗、蓝桥杯），点击跳转官网。
4. **AI+**：链接到 `/ai-plus` 社区页。
5. **团队结构**：6 个组别卡片——机械组、电控组、视觉组、嵌入式组、运营组、ROS 组。
6. **发展历程**：2015→2024 时间线。
7. **铁人协会**：科技交流与实践平台简介。
8. **加入我们**：邮箱、地址、QQ 群、招新二维码。

### 智能车文档结构

`content/docs/smartcar/` 下分四大板块：

- **序章 `prologue/`**：如何提问、学习路线图、硬件入门指引、软件入门指引
- **初级硬件 `junior-hardware/`**：元器件与工具 → 第一块电路 → 第一块 PCB → PCB 结构 → 铺铜 → 项目实战 → PCB 生产 → 通信接口（UART/SPI/I2C）
- **初级软件 `junior-software/`**：介绍 → 开发板选型 → 找文档 → 学习方法 → 继续前行 → 环境搭建 → Git 版本控制
- **24 届考核 `24_assess_hardware/`、`24_assess_software/`**：硬件/软件考核方案

---

## 3. 技术栈

| 类别 | 选型 | 版本（package.json） |
| --- | --- | --- |
| 框架 | Nuxt | ^4.4.8 |
| UI 框架 | Vue | ^3.5.35 |
| 内容引擎 | @nuxt/content | ^3.14.0 |
| 图表 | mermaid | ^11.16.0 |
| 本地内容索引 | better-sqlite3 | ^12.11.1 |
| 路由 | vue-router | ^5.1.0 |
| 包管理器 | pnpm | 见 `pnpm-lock.yaml` |
| 语言 | TypeScript | 见 `tsconfig.json` |
| 站点语言 | 中文（`zh-CN`） | — |
| 字体 | Noto Serif SC / Microsoft YaHei | — |

**说明**：

- 项目采用 **Nuxt 4 的 `app/` 源码目录约定**，页面/组件/布局/中间件/插件都在 `app/` 下。
- `better-sqlite3` 是原生模块，安装时需本地编译（`pnpm` 已在 `pnpm-workspace.yaml` 中通过 `allowBuilds` 放行）。
- 代码高亮由 Nuxt Content 内置的 Shiki 提供，主题 `github-dark`。

---

## 4. 目录结构

```
website/
├── app/                         # Nuxt 4 源码目录
│   ├── app.vue                  # 根组件（NuxtLayout + NuxtPage）
│   ├── assets/css/              # 样式（main.css / style.css）
│   ├── components/              # Vue 组件
│   │   ├── SiteHeader.vue       # 顶部导航
│   │   ├── Alert.vue            # 提示框组件
│   │   └── Counter.vue          # 计数器示例组件
│   ├── layouts/
│   │   └── default.vue          # 默认布局（页头 + 内容 + 页脚）
│   ├── middleware/
│   │   └── docs-prefix.global.ts# /docs/* → /docs/smartcar/* 301 重定向
│   ├── pages/                   # 文件路由
│   │   ├── index.vue            # 首页
│   │   ├── ai-plus.vue          # AI+ 社区页
│   │   ├── [...slug].vue        # 兜底页：按路径渲染 content
│   │   ├── docs/smartcar.vue    # 文档布局（侧栏 + NuxtPage）
│   │   ├── docs/smartcar/index.vue
│   │   ├── docs/smartcar/[...slug].vue
│   │   ├── blog/index.vue       # 博客列表
│   │   └── blog/[...slug].vue   # 博客文章
│   └── plugins/
│       └── code-blocks.client.ts# 客户端插件：Mermaid 渲染 + 代码复制按钮
├── assets/css/style.css         # 额外样式
├── content/                     # ⭐ Markdown 内容源
│   ├── index.md                 # 站点首页内容元数据
│   ├── docs/smartcar/           # 智能车文档
│   └── blog/                    # 博客文章
├── content.config.ts            # Nuxt Content 采集配置
├── public/                      # 静态资源（原样输出到站点根）
│   ├── assets/                  # logo / hero 视频 / 图标 / 二维码
│   ├── content-assets/          # 文档/博客引用的图片
│   └── images/                  # 页面用图（landing / footer / ai-plus 等）
├── nuxt.config.ts               # Nuxt 配置
├── package.json
├── pnpm-lock.yaml
├── pnpm-workspace.yaml
├── tsconfig.json
├── .gitignore
└── README.md                    # Nuxt 官方 starter 说明
```

---

## 5. 本地开发环境准备

### 5.1 前置要求

- **Node.js**：建议 20.x 或以上（Nuxt 4 要求 Node 18+，推荐 LTS）。
- **pnpm**：项目锁文件为 `pnpm-lock.yaml`，请使用 pnpm 安装依赖以锁定版本。

  ```bash
  # 若未安装 pnpm
  corepack enable
  corepack prepare pnpm@latest --activate
  # 或 npm i -g pnpm
  ```

- **原生编译环境**：`better-sqlite3` 需要编译。macOS 需 Xcode Command Line Tools；Linux 需 `python3`、`make`、`g++`；Windows 需 VS Build Tools。通常 pnpm 会优先下载预编译包，无需手动编译。

### 5.2 克隆与安装

```bash
git clone https://github.com/ironspirit-team/website.git
cd website
pnpm install
```

> 首次安装会下载并构建 `better-sqlite3`，请耐心等待。

---

## 6. 本地启动与构建

```bash
# 开发模式（热更新），访问 http://localhost:3000
pnpm dev

# 生产构建
pnpm build

# 生产构建预览
pnpm preview

# 静态站点生成（输出到 .output/public）
pnpm generate
```

| 命令 | 作用 |
| --- | --- |
| `pnpm dev` | 启动开发服务器，修改 Markdown / Vue 实时热更新 |
| `pnpm build` | 生产构建（SSR/Node 服务器） |
| `pnpm generate` | 爬取所有路由生成纯静态站点 |
| `pnpm preview` | 本地预览生产构建产物 |

> 日常写文档只需 `pnpm dev`，保存即可见。

---

## 7. 内容编辑指南

**核心原则**：内容即文件。在 `content/` 下新增 `.md` 文件，站点会自动出现对应页面，无需改动任何代码。

### 7.1 内容采集机制

`content.config.ts` 定义了一个 `content` collection：

```ts
collections: {
  content: defineCollection({
    type: 'page',
    source: '**/*.md',   // 采集 content/ 下所有 Markdown
  }),
}
```

每个 Markdown 文件按其**相对路径**映射为站点路由。例如：

- `content/docs/smartcar/prologue/index.md` → `/docs/smartcar/prologue`
- `content/blog/recruitment-plan/index.md` → `/blog/recruitment-plan`

### 7.2 新增一篇博客

1. 在 `content/blog/` 下新建目录，目录名即 URL（如 `my-post/`）。
2. 在其中创建 `index.md`：

   ```markdown
   ---
   title: "文章标题"
   description: "一句话摘要，会显示在博客列表卡片上"
   excerpt: "可选，列表摘要"
   date: 2026-07-02T10:00:00Z
   lastmod: 2026-07-02T10:00:00Z
   draft: false
   images: ["post.png"]
   categories: ["招新"]
   tags: ["指南", "智能车"]
   contributors: ["铁人战队"]
   pinned: false
   homepage: false
   ---

   ## 正文标题

   正文内容……
   ```

3. 文章内引用的图片放到 `public/content-assets/blog/my-post/`，在 Markdown 中用 `/content-assets/blog/my-post/xxx.png` 引用。

**Frontmatter 字段说明**：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | ✅ | 文章标题 |
| `description` | ✅ | 摘要（列表卡片 / SEO description） |
| `date` | ✅ | 发布时间，列表按此倒序排列 |
| `lastmod` | ❌ | 最后修改时间 |
| `draft` | ❌ | `true` 时不发布 |
| `categories` / `tags` | ❌ | 分类与标签 |
| `contributors` | ❌ | 作者 |
| `pinned` | ❌ | 是否置顶 |
| `images` | ❌ | 封面图（相对文章目录） |

### 7.3 新增一个文档页

1. 在 `content/docs/smartcar/` 对应板块下新建目录 + `index.md`，例如 `content/docs/smartcar/junior-hardware/11-new-chapter/index.md`。
2. Frontmatter 与博客一致（`title` / `description` / `date` 等）。
3. 该页会自动出现在文档页左侧导航栏中（导航由 `app/pages/docs/smartcar.vue` 通过 `queryCollection` 查询 `/docs/smartcar%` 自动生成分组）。
4. 图片放到 `public/content-assets/docs/smartcar/junior-hardware/11-new-chapter/`。

### 7.4 Markdown 扩展能力

- **代码高亮**：用围栏代码块 + 语言标识即可，Shiki 自动着色（预加载 c/cpp/python/bash/shell/json/yaml/typescript/javascript/html/css/markdown/diff/makefile/cmake）。

  ```python
  print("hello ironSpirit")
  ```

- **Mermaid 图表**：用 ` ```mermaid ` 围栏，客户端插件 `app/plugins/code-blocks.client.ts` 会自动将其渲染为 SVG（叠加层方式，不影响源码 DOM）。

  ```mermaid
  flowchart LR
    A[新成员] --> B[序章]
    B --> C[硬件/软件入门]
    C --> D[项目实战]
  ```

- **代码复制按钮**：所有代码块 hover 后右上角出现"复制"按钮，自动复制原文。
- **表格 / 引用 / 行内代码**：均按标准 Markdown 语法，已配样式。

### 7.5 编辑建议

- 修改文档后无需重启，`pnpm dev` 会热更新。
- 新增目录时记得放 `index.md`，否则该路径无内容。
- 图片尽量压缩后再提交，仓库已近 100MB（含大量教学截图）。

---

## 8. 路由与页面机制

| 路由 | 渲染逻辑 |
| --- | --- |
| `/` | `app/pages/index.vue`，纯 Vue 首页（非 content） |
| `/ai-plus` | `app/pages/ai-plus.vue`，纯 Vue 社区页 |
| `/blog` | `blog/index.vue`，`queryCollection` 查询 `/blog/%` 列出全部文章 |
| `/blog/<slug>` | `blog/[...slug].vue`，按路径取 content 并用 `<ContentRenderer>` 渲染 |
| `/docs/smartcar` | `docs/smartcar/index.vue`，渲染文档首页 md |
| `/docs/smartcar/<...>` | `docs/smartcar/[...slug].vue`，渲染对应文档 md；外层 `docs/smartcar.vue` 提供侧栏布局 |
| `/docs` 或 `/docs/<非 smartcar>` | `middleware/docs-prefix.global.ts` 301 重定向到 `/docs/smartcar/...` |
| 其他路径 | `[...slug].vue` 兜底，按路径取 content；找不到则 404 |

**文档侧栏**：`app/pages/docs/smartcar.vue` 调用 `queryCollection('content').where('path','LIKE','/docs/smartcar%')` 取出全部文档页，按 URL 第三段（板块名）自动分组成可折叠导航。新增文档会自动入列，无需手改导航。

---

## 9. 样式与主题

- **全局样式**：`nuxt.config.ts` 中 `css: ['~/assets/css/main.css', '~/assets/css/style.css']`（`~` 指向 `app/`）。
- **主色调**：墨绿 `#0b6b63` / 深绿 `#17211f`，背景米白 `#f7f4ee`，字体 Noto Serif SC。
- **代码块**：深色背景 `#1c2128`，github-dark 主题。
- **响应式断点**：860px / 900px / 640px / 420px，移动端侧栏自动改为单列堆叠。
- **页面专属样式**：`ai-plus.vue` 内含 `<style scoped>`；文档/博客页背景使用 `public/images/section-bg-repeat-y-car.png` 平铺。

---

## 10. 部署

仓库内未包含部署配置文件（无 `vercel.json` / `netlify.toml` / `Dockerfile`），可按 Nuxt 官方部署文档选择方案：

### 方案 A：静态托管（推荐）

```bash
pnpm generate
# 产物在 .output/public/，上传到任意静态托管（Vercel / Netlify / Cloudflare Pages / GitHub Pages / 对象存储）
```

适合本站：内容以 Markdown 为主，可静态化。

### 方案 B：Node SSR

```bash
pnpm build
# 产物在 .output/，运行 node .output/server/index.mjs
```

### 方案 C：托管平台自动构建

- **Vercel**：导入仓库，框架选 Nuxt，构建命令 `pnpm generate`，输出目录 `.output/public`。
- **Netlify**：构建 `pnpm generate`，发布目录 `.output/public`。
- **GitHub Pages**：用 `pnpm generate` 后通过 Actions 推送 `.output/public` 到 `gh-pages` 分支。

> 注意 `better-sqlite3` 为原生模块，静态生成（`generate`）时仅在构建期使用，产物不依赖它；若选 SSR 部署需确保运行环境兼容。

---

## 11. 常见问题与维护建议

**Q: `pnpm install` 报 `better-sqlite3` 编译失败？**
A: 确保已装编译工具链（macOS: `xcode-select --install`；Linux: `build-essential` + `python3`）。也可尝试 `pnpm rebuild better-sqlite3`。

**Q: 新增的 Markdown 没有出现在站点？**
A: 检查文件是否在 `content/` 下、扩展名是否 `.md`、frontmatter 是否有 `title`。`draft: true` 的不会发布。

**Q: 文档左侧导航没显示新页面？**
A: 导航按路径前缀 `/docs/smartcar` 自动生成。新页面路径需在此前缀下；若新建了板块目录，确认其中有 `index.md`。

**Q: Mermaid 图表不渲染？**
A: 客户端插件在 `onNuxtReady` 后延迟 300ms 处理，首次进入稍有延迟属正常。若一直不渲染，检查浏览器控制台是否有 `[Mermaid] 渲染失败` 警告，多为语法错误。

**Q: 图片显示不出来？**
A: 站点图片统一放 `public/` 下并用绝对路径 `/...` 引用。文档/博客配图分别放 `public/content-assets/docs/...` 与 `public/content-assets/blog/...`。

**维护建议**：

- `README.md` 当前仍是 Nuxt starter 默认英文内容，建议替换为中文项目说明或指向本文件。
- 仓库体积偏大（大量教学截图），后续可考虑接入图片 CDN 或 Git LFS。
- 建议在仓库根补充 `LICENSE` 与贡献指南 `CONTRIBUTING.md`。

---

## 12. 联系方式

| 项 | 内容 |
| --- | --- |
| 邮箱 | ironspirit_rm@swpu.edu.cn |
| 地址 | 四川省成都市新都区西南石油大学明辨楼 C205 |
| QQ 群 | 863606991 |
| GitHub | https://github.com/ironspirit-team |
| Bilibili | https://space.bilibili.com/2138265818 |

---

*Engineered to Inspire · ironSpirit Team · 2015–2026*
